#ifndef WASM_H
#define WASM_H
// This is a rather low priority backend considering that LLVM already supports WebAssembly decently well.
// However, LLVM tends to be behind when it comes to WebAssembly advancements and it relies on linear memory.
// Strawberry as a language does not actually rely on it and could more seamlessly take advantage of the platform.

#endif
